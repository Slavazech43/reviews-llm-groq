# 📊 Audience Lens Dashboard - Инструкция

## 🚀 Быстрый старт

### 1. Обновите React компоненты

```bash
# Скопируйте файлы в ваш проект
cp App.jsx audience-lens-app/src/App.jsx
cp App.css audience-lens-app/src/App.css
```

### 2. Скопируйте JSON файлы в dashboard

```bash
# Автоматическое копирование
python update_dashboard.py
```

Или вручную:
```bash
cp audience_analysis_results.json audience-lens-app/public/
cp product.json audience-lens-app/public/
cp reviews.json audience-lens-app/public/
```

### 3. Запустите dashboard

```bash
cd audience-lens-app
npm run dev
```

Откройте в браузере: http://localhost:5173

---

## 📁 Структура файлов

```
audience-lens-app/
├── public/
│   ├── audience_analysis_results.json  ← Результаты анализа
│   ├── product.json                    ← Данные о товаре
│   └── reviews.json                    ← Отзывы
├── src/
│   ├── App.jsx                         ← Главный компонент (ОБНОВЛЕН)
│   └── App.css                         ← Стили (ОБНОВЛЕН)
└── package.json
```

---

## 🔄 Автоматическое обновление данных

### Вариант 1: Скрипт update_dashboard.py

```bash
# После каждого анализа просто запустите:
python update_dashboard.py
```

Скрипт автоматически:
- ✅ Найдет папку dashboard
- ✅ Скопирует все JSON файлы
- ✅ Покажет статус

### Вариант 2: Bash скрипт

Создайте `update.sh`:
```bash
#!/bin/bash
cp *.json audience-lens-app/public/
echo "✅ Файлы обновлены!"
```

Запуск:
```bash
chmod +x update.sh
./update.sh
```

---

## 🎯 Полный пайплайн

```bash
# 1. Создание JSON файлов
python final_json_generator.py

# 2. Анализ отзывов
python reviews_groq_criteria.py

# 3. Анализ аудитории
python audience_analysis_groq.py --product product.json --reviews reviews.json

# 4. Обновление dashboard
python update_dashboard.py

# 5. Запуск (если еще не запущен)
cd audience-lens-app && npm run dev
```

---

## ✨ Что нового в dashboard

### Автоматическая загрузка данных
- 🔄 Кнопка "Обновить" в header
- 📡 Загрузка JSON из папки public/
- 🎨 Fallback на mock данные если файлов нет

### 4 вкладки

#### 📊 Обзор
- Карточки с основными метриками
- Круговая диаграмма сегментов
- График оценок по критериям

#### 👥 Сегменты
- Подробные карточки для каждого сегмента
- Боли и желания
- Оценки по критериям

#### 💬 Отзывы
- Список всех отзывов
- Фильтры: все / положительные / отрицательные
- ID и текст отзыва

#### 💡 Рекомендации
- Нумерованный список
- Детальное описание
- Ожидаемый эффект

---

## 🔧 Настройка

### Изменить порт

В `audience-lens-app/vite.config.js`:
```js
export default defineConfig({
  server: {
    port: 3000  // Вместо 5173
  }
})
```

### Добавить новые графики

В `App.jsx` используйте Recharts:
```jsx
import { LineChart, Line, AreaChart, Area } from 'recharts'
```

---

## 📊 Формат данных

### audience_analysis_results.json
```json
{
  "product": {
    "id": "wb_drill",
    "name": "Название товара"
  },
  "segments": [
    {
      "name": "Сегмент 1",
      "size": 45,
      "percentage": 45,
      "description": "Описание",
      "pain_points": ["Боль 1", "Боль 2"],
      "desires": ["Желание 1"],
      "criteria_scores": {
        "quality": 4.2,
        "price": 4.5
      }
    }
  ],
  "recommendations": [
    "Рекомендация 1",
    "Рекомендация 2"
  ]
}
```

### product.json
```json
[{
  "id": "wb_drill",
  "name": "Название",
  "price": 1298,
  "currency": "RUB",
  "description": "Описание",
  "characteristics": "Характеристики"
}]
```

### reviews.json
```json
[{
  "id": "wb_1",
  "product_id": "wb_drill",
  "review": "Текст отзыва"
}]
```

---

## 🐛 Решение проблем

### Dashboard не видит файлы

1. Убедитесь что файлы в `public/`:
```bash
ls -la audience-lens-app/public/
```

2. Перезапустите dev server:
```bash
cd audience-lens-app
npm run dev
```

3. Обновите страницу с очисткой кеша: `Cmd+Shift+R` (Mac) или `Ctrl+Shift+R` (Windows)

### Данные не отображаются

Откройте консоль браузера (`F12`) и проверьте ошибки. Скорее всего:
- Неправильный формат JSON
- Файлы не в `public/`
- Ошибка в путях

### Стили не применяются

```bash
# Убедитесь что App.css импортирован в App.jsx
import './App.css'
```

---

## 🎨 Кастомизация

### Изменить цвета

В `App.css`:
```css
.app-header {
  background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}

.tab.active {
  background: #your-color;
}
```

### Добавить новую вкладку

В `App.jsx`:
```jsx
// 1. Добавить кнопку
<button 
  className={`tab ${activeTab === 'new' ? 'active' : ''}`}
  onClick={() => setActiveTab('new')}
>
  🆕 Новая вкладка
</button>

// 2. Добавить контент
{activeTab === 'new' && <NewTab data={data} />}

// 3. Создать компонент
function NewTab({ data }) {
  return <div className="tab-content">Ваш контент</div>
}
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (`F12`)
2. Проверьте формат JSON файлов
3. Убедитесь что все файлы на месте
4. Перезапустите dev server

---

## ✅ Чек-лист

- [ ] App.jsx скопирован в `src/`
- [ ] App.css скопирован в `src/`
- [ ] JSON файлы в `public/`
- [ ] `npm run dev` запущен
- [ ] Страница открыта в браузере
- [ ] Нет ошибок в консоли

---

Готово! 🎉 Ваш dashboard теперь автоматически загружает данные из JSON!
